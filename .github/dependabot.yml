version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # eslint-config-next 15 supports ESLint through v9 only.
      - dependency-name: "eslint"
        update-types:
          - "version-update:semver-major"
